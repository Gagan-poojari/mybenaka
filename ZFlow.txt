1. Frontend:
    1.0. Installation
    1.1. Landing Page

2. Backend:
    2.0. Installation
    2.1. MongoDB atlas database creation and setup
    2.2. Folder Structure (configs, controllers, middlewares, models, routes, utils)
    DB CONNECTION:
        2.3. configs/db.js -> Database connection
        2.4. Add db.js to index.js
    SIGNUP / LOGIN:
        2.5. Create user model -> models/user.model.js
        2.6. Generate jwt token -> utils/generateToken.js
        2.7. Create auth controller -> controllers/auth.controller.js
        2.8. Create routes -> routes/auth.route.js
        2.9. Add routes to index.js
        2.10. Create verify token middleware -> middlewares/verifyToken.js
    ACCESS USER DATA FROM TOKEN:
        2.11. Create user controller -> controllers/user.controller.js
        2.12. Create user routes -> routes/user.route.js
        2.13. Add routes to index.js

3. Frontend:
    ACCESS USER DATA FROM TOKEN:
        3.1. Create user context -> contexts/user.context.js
    PROTECTED ROUTES (/home, /dashboard): 
        3.2. Use UserContext.jsx to access user data and render protected routes
        3.3. Redirect to login page if user is not logged in

4. Frontend:
    SIGNUP / LOGIN:
        4.0. Wrap layout.js with UserContext and AuthContext
        4.1. Create signup and login pages
        4.2. Take user input and send to backend
        4.3. Create auth context -> contexts/auth.context.js

    DASHBOARDS:
        4.4. Create Dummy data for Frontend 
        4.5. Create dashboard pages for 3 roles (admin, user, manager)

5. Backend:
    DASHBOARDS:
        BACKEND: TO CRUD BORROWER DETAILS BY THE ADMIN 
        5.1. Create borrower model -> models/borrower.model.js
        5.2. Create a controller to handle CRUD operations of borrowers -> controllers/borrower.controller.js
        5.3. Create routes for borrowers -> routes/borrower.route.js
        5.4. Add routes to index.js
        FRONTEND:
        5.5. Create /dashboard/admin/page.jsx



For the current scope, you’ve basically covered all critical points:
    Admin: CRUD for borrowers, manual payment updates, interest updates.
    User: Personal loan dashboard with detailed stats.
    Manager: Placeholder until roles are defined.

Things to consider later (optional but useful):
    Overdue notifications / reminders.
    Scheduled automatic interest calculation.
    Partial payments and interest recalculation.
    Loan history / archives.


TODO: 
CHECK REPAYMENT CONTROLLER'S RECORD PAYMENT
DATE STORAGE ERROR